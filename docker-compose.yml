services:
  postgres:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: kim
      POSTGRES_PASSWORD: Fdmmda778778
      POSTGRES_DB: assettracker
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
